/* The share card is one design; X and WhatsApp-style unfurlers just read
 * different tags for it. Re-exported rather than duplicated. */
export { alt, size, contentType, default } from "./opengraph-image";
